version: 2
jobs:
  abas:
    docker:
      - image: python:3.8

    steps:
    - checkout
    - run: ls && ls
    - run: ls && ls
    - run: ls && ls
    - run: sleep 2s && date
    - run: ls && sleep 2s


workflows:
  version: 2
  abas:
    jobs:
    - abas
